[project]
name = "sftp-connector-lambdas-workspace"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["joiner", "event-writer"]

[dependency-groups]
dev = [
    "pytest>=9.0,<10",
    "pytest-mock>=3.15,<4",
]
